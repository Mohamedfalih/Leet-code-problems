int removeElement(int* nums, int numsSize, int val)
{
    if(numsSize==0)return 0;
    int i=0;
    for(int j=0;j<numsSize;j++)
    {
        if(val!=nums[j])
        {
            nums[i]=nums[j];
            i++;
        }
    }
    return i;
}
