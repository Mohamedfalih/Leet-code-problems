QUESTION:
Given an integer array nums, find three numbers whose product is maximum and return the maximum product.

SOURCE CODE:
class Solution {
public:
    int maximumProduct(vector<int>& nums)
    {
        int size = nums.size();
        sort(nums.begin(),nums.end());

        int prod_1 = nums[size-1]*nums[size-2]* nums[size-3];
        int prod_2 = nums[0]*nums[1]*nums[size-1];

        return max(prod_1,prod_2);
    }
};

OUTPUT:
Example 1:

Input: nums = [1,2,3]
Output: 6
Example 2:

Input: nums = [1,2,3,4]
Output: 24
Example 3:

Input: nums = [-1,-2,-3]
Output: -6
